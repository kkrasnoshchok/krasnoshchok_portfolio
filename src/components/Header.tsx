import React, { FC } from "react";
import { SHeader, SHeaderTitle } from "../styles/scomponent/sheader";

export const Header: FC = () => {
  return (
    <SHeader>
      <SHeaderTitle href="#">krasnoshchok</SHeaderTitle>
    </SHeader>
  );
};
